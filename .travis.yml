language: python
python:
  - '3.7'

before_script:
  - pip install -r requirements.txt

branches:
  only:
    - staging

env:
  - DJANGO_SETTINGS_MODULE="pur_beurre_project.settings.travis"
  - TRAVIS=true
  
services:
  - postgresql

script:
  - ./manage.py test
